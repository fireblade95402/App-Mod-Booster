create all the apis needed to fulfil the functionality in the screenshots and use those in the app code
they wont talk to the database yet so make sure they return dummy data
also create the swagger docs so we can see these apis listed in the repo with buttons to test them as just APIs if needed
